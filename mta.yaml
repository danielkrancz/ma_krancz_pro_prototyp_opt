_schema-version: "3.2"
ID: athbmakranczprocodeappopt
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: athbmakranczprocodeappopt-destination-content
  type: com.sap.application.content
  requires:
  - name: athbmakranczprocodeappopt-destination-service
    parameters:
      content-target: true
  - name: athbmakranczprocodeappopt-repo-host
    parameters:
      service-key:
        name: athbmakranczprocodeappopt-repo-host-key
  - name: athbmakranczprocodeappopt-uaa
    parameters:
      service-key:
        name: athbmakranczprocodeappopt-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: athbmakranczprocodeappopt_html_repo_host
          ServiceInstanceName: athbmakranczprocodeappopt-html5-service
          ServiceKeyName: athbmakranczprocodeappopt-repo-host-key
          sap.cloud.service: athbmakranczprocodeappopt
        - Authentication: OAuth2UserTokenExchange
          Name: athbmakranczprocodeappopt_uaa
          ServiceInstanceName: athbmakranczprocodeappopt-xsuaa-service
          ServiceKeyName: athbmakranczprocodeappopt-uaa-key
          sap.cloud.service: athbmakranczprocodeappopt
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: athbmakranczprocodeappopt-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: athbmakranczprocodeappopt-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - athbmakranczprocodeappopt.zip
      name: athbmakranczprocodeappopt
      target-path: resources/
- name: athbmakranczprocodeappopt
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: athbmakranczprocodeappopt-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: athbmakranczprocodeappopt-destination-service
    service-plan: lite
- name: athbmakranczprocodeappopt-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: athbmakranczprocodeappopt-xsuaa-service
    service-plan: application
- name: athbmakranczprocodeappopt-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: athbmakranczprocodeappopt-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
